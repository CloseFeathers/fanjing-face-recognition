我要新增一个独立于身份识别的人脸“说话状态”功能，目标不是做简单的嘴开闭检测，而是做 **video-only visual speaking detection**，同时单独判断 **occluded（嘴部被遮挡/不可判）**，用于后续更好地和声纹系统协同判断谁在说话。请按当前较强路线来做，不要走“先判张嘴/闭嘴，再靠硬规则拼说话”的低阶方案，而是采用 **visibility-aware dense mouth landmarks（密集嘴部关键点 + 每个关键点可见性） + aligned mouth ROI + temporal speaking model + occlusion/observability side head** 的方案。这个分支要挂在 **M2 跟踪之后**，并且 **与 M3/M4/M5 并行**，按 `track_id` 工作，不等待 `person_id` 或 M5 身份状态；因为 speaking 是当前可见目标的时序属性，不是身份属性。对每个稳定的 confirmed track，从原始帧裁高分辨率 face crop，不复用现有给 ArcFace 用的 112×112 identity 对齐图；然后新增一个嘴部前端，输出：密集嘴部关键点、每点可见性、头部姿态，并据此生成对齐后的 mouth ROI 序列。为每个 track 维护一个短时环形缓存，保存一小段时间窗内的 `mouth ROI + landmarks + visibility + pose + quality + timestamp`。真正的判断顺序必须是：**先做可判性/遮挡判断，再做 speaking 判断**；也就是先判断这段嘴部信息是否足够可靠，如果嘴部被手/口罩/杯子遮挡、侧脸自遮挡严重、分辨率太低或清晰度太差，就输出 `occluded`，而不是强行判成 `not_speaking`；只有在“可判”的前提下，才把这段时间窗交给时序 speaking expert，直接输出 `speaking_prob`。不要用简单的 mouth-open/mouth-close 阈值作为最终裁决，张嘴闭嘴只能作为辅助特征之一，因为“说话”本质是一个时序口型运动模式，不是单帧几何状态。对外最终输出三态：`speaking / not_speaking / occluded`；对内请保留 `speaking_prob / occlusion_prob / observable_prob / confidence / reason_code`。整个 speaking branch 必须是 **异步 latest-only** 的，按 track 做独立缓存和限频，不能阻塞主循环、不能拖慢前端 FPS；主线程只负责把当前 track 的 face crop 投递给 mouth worker，并读取最近一次可用结果回写到 TrackState。请先基于这个方向给我：1）完整数据流和模块拆分；2）关键数据结构；3）推理顺序和状态机；4）性能保护方案；5）最小可实施版本与后续增强版本，再开始实现。