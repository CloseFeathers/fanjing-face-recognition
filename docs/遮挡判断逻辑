1. 头部姿态（yaw / pitch）

这个必须保留，而且我会把它提升成硬规则。

因为它解决的是自遮挡，这是几何事实，不是统计猜测。
但它只能解决“看不全”的一部分，不能解决手、口罩、杯子这些外部遮挡。所以它应该是：

强阳性证据：姿态太极端，直接判 mouth unavailable
2. 嘴部 ROI 纹理复杂度

这个能用，但只能是弱证据。
3. 颜色 / 红色通道比例

我不建议把这个当主信号。
最大问题不是“调参麻烦”，而是它本质上太脆弱：
光照
白平衡
唇色差异
肤色差异
口红 / 胡子 / 阴影
而且手挡嘴这个最重要场景，本来就很难靠颜色解决。Hand2Face 很早就明确指出：手和脸在颜色、纹理上本来就很像，这正是 hand-over-face occlusion 难的原因之一。

我觉得 Cursor 方案里缺的，反而是下面这几类更关键的信号
1. 关键点-图像一致性（landmark-image agreement）

这是我最推荐你加进去的一条。

思路很简单：
如果嘴部关键点真的落在“可见的嘴唇边界”上，那么在这些点附近，图像里应该存在：

合理的边缘
合理的亮度/颜色变化
合理的轮廓支持

如果关键点落在某个位置，但周围根本没有与嘴唇边界相匹配的图像证据，那这些点大概率是模型在猜，或者该区域已经被挡住了。

这条思路很强，因为它不靠“嘴是不是红色”，而是靠：

模型预测的嘴轮廓，是否真的被图像支持

这和 boundary-aware landmark 研究的精神很接近：边界信息能减少 landmark 定义歧义，并且在 occlusion benchmark 上更稳。

这条具体怎么做

对嘴部外轮廓、内轮廓的每个关键点，算一个局部支持分：

该点附近沿法线方向的梯度强度
边界两侧的对比度
与临近 contour 点形成的连续边界强度

这样每个点都会得到一个 support_i。
然后你就可以得到一个非常接近“手工 visibility”的量：

visible_ratio = mean(support_i > τ)
max_missing_arc = 连续低支持点的最长弧段

这个东西比单纯的 z 值、颜色值都强很多。

2. 嘴唇语义分割 / face parsing 重叠度

这是第二条我非常推荐的。

既然你现在没有现成的 visibility 输出，那就换个角度问：

这块区域里，语义上到底像不像“嘴唇/口腔”？

做法是：

跑一个 face parsing / lip segmentation 模块
得到 lip mask / mouth region probability
再和 landmarks 定义的 mouth polygon 做 overlap

如果：

landmark 说“这里应该是嘴”
但 parser 长期说“这里根本不像嘴唇，而像皮肤/物体/背景”

那就很可能已经被遮挡了，或者根本不可用。

这条路线的好处是，它比颜色 heuristic 更语义化，也更能跨肤色、光照和个体差异。研究上，FaceXFormer 这种统一模型本身就把 face parsing + visibility + landmarks + pose 当成同一类任务；这其实就是在告诉你：语义分割和可见性本来就是强相关证据。

4. 伪 per-point visibility（pseudo visibility）

这是我觉得你现在最该做的“高级版本核心”。

既然前端模型不会直接给你 visibility，那你就自己“造”一个。

方法不是单独训练 visibility 模型，而是对每个嘴部关键点，融合多种局部证据，得到一个 v_i：

pose prior（该点是否落在可能被自遮挡的一侧）
contour support（该点是否有边界支持）
parser support（该点附近是否被分到 lips/mouth）
quality support（该点附近是否太糊/太暗）
temporal support（该点在最近几帧是否持续得到支持）

最后得到：

每点 visibility
上唇可见率
下唇可见率
嘴角可见率
连续被遮挡弧段长度

这个东西非常像 OccFace 那种 per-point visibility 思想，只不过是你们在系统层自己拼出来的。
它的好处是：侧脸 bias 会明显小很多，因为你不是再问“整张嘴可不可见”，而是在问“哪一部分嘴还可信”。

5. 时序持久性，不是“稳定性”本身

我建议把 Cursor 原来的“帧间关键点稳定性”改造成：

遮挡证据是否持续存在

也就是你不是看“点稳不稳”，而是看：

低可见性是否连续出现
parser 不支持是否连续出现
occluder overlap 是否持续
contour support 是否持续缺失

这条的作用不是检测，而是防抖 + 减少单帧误报。

组合方式
所以，我会怎么组合这些方法

我不会用 Cursor 现在那种：

0.4纹理 + 0.3颜色 + ...

这种固定手工打分长期当主方案。
我会改成下面这套分层结构。

第一层：先做 observability gate

先把这些“根本不该判”的情况挡掉：

嘴部 ROI 太小
太糊
太暗 / 太曝
超出边界

这层不是在判遮挡，而是在判：
当前嘴部图像有没有分析价值。

第二层：再做 自遮挡 hard gate

只看 pose：

|yaw| 太大
pitch 太极端

这时直接判：

self_occluded

这一层是最硬的规则。

第三层：外部遮挡证据融合

这层才去融合真正的多种外部遮挡证据：

强证据
occluder_overlap
lip_parser_overlap 很低
contour_support 很差
弱证据
texture_complexity 低
color_contrast 异常
z_residual 异常

注意这里颜色和纹理只做弱证据，不许单独拍板。

第四层：track 级时间融合

按 track_id 做短时窗融合：

连续 N 帧都支持遮挡，才提升为 occluded
连续 N 帧都恢复可见，才解除 occluded

这一步最适合你当前系统，因为你本来就有稳定 track。

小建议：
还有一个我很建议你做的工程决策

不要永久手调权重。
手工规则可以拿来启动系统，但最后最好把这些特征喂给一个很小的融合器，比如：

Logistic Regression
XGBoost / LightGBM
小 MLP

输入就是你上面这些 feature。
原因很简单：固定的 +0.4 / +0.3 在不同摄像头、光照、肤色、分辨率下非常容易失效。

也就是说：

规则 用来定义初版逻辑
小模型 用来学最终融合

这个会比长期手调阈值靠谱得多。